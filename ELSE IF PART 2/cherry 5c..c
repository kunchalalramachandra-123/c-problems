#include <stdio.h>
int main() {
    float data;
    scanf("%d",&data);
    if (data <2.5)
    printf("data limit exceeded");
    else if (data && 1.8)
    printf("data within limit");
    else
    printf("data within limit");
     return 0;
    }