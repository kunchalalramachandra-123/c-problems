#include <stdio.h>
int main() {
  int amount;
   scanf("%d",&amount); 
   if (amount > 500)
   printf("valid amount");
   else if (amount <= 450)
   printf("invalid amount");
   else 
   printf("valid amount");
    return 0;
}