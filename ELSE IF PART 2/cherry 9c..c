#include <stdio.h>
int main() {
    int speed;
    scanf("%d",&speed);
    if (speed>=90)
    printf("speed limit exceeded");
    else if(speed= 60)
    printf("within speed limit");
    else
    printf("within speed limit");
    return 0;
}